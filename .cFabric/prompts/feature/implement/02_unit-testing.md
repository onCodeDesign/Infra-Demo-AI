Use Mode 2 to create Unit test for feature #{issueId} as specified into the detailed design from docs/workitems/{issueId}-detailed-design.md. When you finish append the HANDOFF block exactly as in the format in #Completion Protocol
Context:
Mode=2
Files={file-list}
Build={build-status}
Tests={test-status}
Deviations={deviations}
Commits={commit-list}